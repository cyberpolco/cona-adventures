# Prisma reads this file. Next.js reads both .env and .env.local (.env.local wins).
# DATABASE_URL here is safe for SQLite (local file); swap to Postgres before production.
DATABASE_URL="file:./dev.db"
